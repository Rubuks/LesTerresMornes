/* ============================================================
LA GRANDE ROUTE — src/legacy.js  (moteur pur, tout en clés i18n)
Socles 1+2+3 + fusion : sac HUD, grimoire bestiaire persistant,
événements uniques, bague bloquée après quête/colonne 5, drapeaux
masqués dès "Commencer le voyage", villages-goulets (12 colonnes),
quête de la pendue (renommée ±), ralliement du guerrier nekker
(invocation), spéciaux épiques actifs, renommée négative = malus.
============================================================ */
const R=(a,b)=>Math.floor(Math.random()*(b-a+1))+a;
const pick=a=>a[Math.floor(Math.random()*a.length)];
const $=id=>document.getElementById(id);
const ec=new Proxy($('ecran'),{get(o,k){const v=Reflect.get(o,k);return typeof v==='function'?v.bind(o):v;},set(o,k,v){if(k==='innerHTML')v=decorate(v);o[k]=v;return true;}});
let AC=null;
function sfx(t){try{AC=AC||new (window.AudioContext||window.webkitAudioContext)();const o=AC.createOscillator(),g=AC.createGain();o.connect(g);g.connect(AC.destination);const n=AC.currentTime;const P={clic:[620,'square',.04,.06],hit:[130,'sawtooth',.14,.22],crit:[85,'sawtooth',.22,.32],signe:[420,'sine',.13,.28],coin:[880,'triangle',.09,.18],soin:[520,'sine',.11,.32],mort:[70,'sawtooth',.35,.65],cor:[200,'square',.25,.5]}[t]||[440,'sine',.05,.1];o.type=P[1];o.frequency.setValueAtTime(P[0],n);if(t==='signe'||t==='mort')o.frequency.exponentialRampToValueAtTime(P[0]*(t==='mort'?.3:2.4),n+P[3]);g.gain.setValueAtTime(P[2],n);g.gain.exponentialRampToValueAtTime(.0001,n+P[3]);o.start(n);o.stop(n+P[3]+.03);}catch(e){}}
const IMG={
hero:{loup:'assets/hero_loup.jpg',sorciere:'assets/hero_sorciere.jpg',mercenaire:'assets/hero_mercenaire.jpg',eclaireuse:'assets/hero_eclaireuse.jpg'},
monster:{noyeur:'assets/m_noyeur.jpg',goule:'assets/m_goule.jpg',nekkere:'assets/m_nekkere.jpg',loup:'assets/m_loup.jpg',foglet:'assets/m_foglet.jpg',brigand:'assets/m_brigand.jpg',spectre:'assets/m_spectre.jpg',fielon:'assets/m_fielon.jpg',sylvestre:'assets/m_sylvestre.jpg',griffon:'assets/m_griffon.jpg',chien:'assets/m_chien.jpg',pourrissoir:'assets/m_pourrissoir.jpg',alguire:'assets/m_alguire.jpg',guerrier_nekker:'assets/m_guerrier_nekker.jpg'},
place:{titre:'assets/place_titre.jpg',village:'assets/place_village.jpg',nid:'assets/place_nid.jpg',map:'assets/map_velen.jpg',cin_title:'assets/cin_title.jpg',cin_marsh:'assets/cin_marsh.jpg',cin_village:'assets/cin_village.jpg',cin_nest:'assets/cin_nest.jpg'},
npc:{ealdorman:'assets/npc_ealdorman.jpg',herboriste:'assets/npc_herboriste.jpg',woodcutter:'assets/npc_woodcutter.jpg'},
item:{v_heal:'assets/v_heal.png',v_vigor:'assets/v_vigor.png',v_mist:'assets/v_mist.png',v_bomb:'assets/v_bomb.png',v_toxin:'assets/v_toxin.png',v_oil:'assets/v_oil.png',v_crystal:'assets/v_crystal.png',v_glyph:'assets/v_glyph.png',v_mug:'assets/v_mug.png',v_mead:'assets/v_mead.png',v_rune:'assets/v_rune.png',v_bolt:'assets/v_bolt.png',v_herb:'assets/v_herb.png'}
};
function portrait(kind,key,emoji,cls,style){const src=IMG[kind]&&IMG[kind][key];if(!src)return `<span class="${cls||''}" style="${style||''}">${emoji}</span>`;return `<span class="pw ${cls||''}" style="${style||''}"><img src="${src}" alt="" loading="lazy" onerror="this.classList.add('x')"><span class="fb">${emoji}</span></span>`;}
function bg(kind,key){const src=IMG[kind]&&IMG[kind][key];return src?`<div class="ecbg" style="background-image:url('${src}')"></div>`:'';}
function cineBg(key){const src=IMG.place[key];return src?`<div class="ecbg cine" style="background-image:url('${src}')"></div>`:'';}
const vignette=(key,em,cls)=>portrait('item',key,em,cls);
const SIGNES={aard:{n:'w.sign.aard.name',em:"🌀",d:'w.sign.aard.desc'},igni:{n:'w.sign.igni.name',em:"🔥",d:'w.sign.igni.desc'},quen:{n:'w.sign.quen.name',em:"🛡️",d:'w.sign.quen.desc'},yrden:{n:'w.sign.yrden.name',em:"🪤",d:'w.sign.yrden.desc'},axii:{n:'w.sign.axii.name',em:"💫",d:'w.sign.axii.desc'}};
const CLASSES={
loup:{n:'w.class.loup.name',ec:'w.class.loup.school',em:"🐺",coul:"#aeb6bf",pv:72,atq:[8,12],esq:15,distance:false,signes:['aard','igni','quen','yrden','axii'],spec:'w.class.loup.spec',desc:'w.class.loup.desc',cri:'w.class.loup.cri',depart:{hirondelle:2,foudre:1},st:{PV:72,Frappe:10,Magie:9,Esquive:15}},
sorciere:{n:'w.class.sorciere.name',ec:'w.class.sorciere.school',em:"🔮",coul:"#6f8fb8",pv:52,atq:[5,8],esq:10,distance:false,signes:['aard','igni','quen','yrden','axii'],meteore:true,igniBoost:true,spec:'w.class.sorciere.spec',desc:'w.class.sorciere.desc',cri:'w.class.sorciere.cri',depart:{nectar:2,poudre:1},st:{PV:52,Frappe:6,Magie:15,Esquive:10}},
mercenaire:{n:'w.class.mercenaire.name',ec:'w.class.mercenaire.school',em:"🪓",coul:"#c07a3c",pv:88,atq:[11,15],esq:8,distance:false,signes:[],criAction:true,spec:'w.class.mercenaire.spec',desc:'w.class.mercenaire.desc',cri:'w.class.mercenaire.cri',depart:{esprit:2,kvas:1,grappin:1},st:{PV:88,Frappe:13,Magie:2,Esquive:8}},
eclaireuse:{n:'w.class.eclaireuse.name',ec:'w.class.eclaireuse.school',em:"🏹",coul:"#8fae6a",pv:58,atq:[7,10],esq:22,distance:true,signes:['quen','axii'],spec:'w.class.eclaireuse.spec',desc:'w.class.eclaireuse.desc',cri:'w.class.eclaireuse.cri',depart:{mixture:2,seve:1},st:{PV:58,Frappe:9,Magie:5,Esquive:22}}
};
const NOMS={frappe:{loup:'w.line.strike.loup',sorciere:'w.line.strike.sorciere',mercenaire:'w.line.strike.mercenaire',eclaireuse:'w.line.strike.eclaireuse'},lourde:{loup:'w.line.heavy.loup',sorciere:'w.line.heavy.sorciere',mercenaire:'w.line.heavy.mercenaire',eclaireuse:'w.line.heavy.eclaireuse'}};
const TEXTES={
loup:{degaine:['w.text.loup.draw0','w.text.loup.draw1','w.text.loup.draw2'],victoire:['w.text.loup.win0','w.text.loup.win1','w.text.loup.win2'],village:'w.text.loup.village',boss:'w.text.loup.boss'},
sorciere:{degaine:['w.text.sorciere.draw0','w.text.sorciere.draw1','w.text.sorciere.draw2'],victoire:['w.text.sorciere.win0','w.text.sorciere.win1','w.text.sorciere.win2'],village:'w.text.sorciere.village',boss:'w.text.sorciere.boss'},
mercenaire:{degaine:['w.text.mercenaire.draw0','w.text.mercenaire.draw1','w.text.mercenaire.draw2'],victoire:['w.text.mercenaire.win0','w.text.mercenaire.win1','w.text.mercenaire.win2'],village:'w.text.mercenaire.village',boss:'w.text.mercenaire.boss'},
eclaireuse:{degaine:['w.text.eclaireuse.draw0','w.text.eclaireuse.draw1','w.text.eclaireuse.draw2'],victoire:['w.text.eclaireuse.win0','w.text.eclaireuse.win1','w.text.eclaireuse.win2'],village:'w.text.eclaireuse.village',boss:'w.text.eclaireuse.boss'}
};
const PROLOGUES={loup:'w.prologue.loup',sorciere:'w.prologue.sorciere',mercenaire:'w.prologue.mercenaire',eclaireuse:'w.prologue.eclaireuse'};
const KEY='lgr_v5';
const KEY_BEST='lgr_best';
const BELT_MAX=3;
let S=null,C=null,ND=null,EV=null,CURCLS=null,FX=[];
let _FROM_SAC=null,_FROM_BEST=null;
let BEST=chargerBest();
function chargerBest(){try{return JSON.parse(localStorage.getItem(KEY_BEST))||[]}catch(e){return[]}}
function sauverBest(){try{localStorage.setItem(KEY_BEST,JSON.stringify(BEST))}catch(e){}}
function aggregateWorn(){const b={pvFlat:0,esqFlat:0,armorStart:0,paFirst:0,dmgCat:{}};(S&&S.worn||[]).forEach(id=>{const T=(typeof TROPHIES!=='undefined')?TROPHIES[id]:null;const bn=T&&T.bonus;if(!bn)return;b.pvFlat+=bn.pvFlat||0;b.esqFlat+=bn.esqFlat||0;b.armorStart+=bn.armorStart||0;b.paFirst+=bn.paFirst||0;if(bn.dmgCat)for(const k in bn.dmgCat)b.dmgCat[k]=(b.dmgCat[k]||1)*bn.dmgCat[k];});return b;}
function recalcTrophyStats(){if(!S)return;const base=CLASSES[S.cls].pv;const b=aggregateWorn();S.pvMax=base+b.pvFlat;if(S.pv>S.pvMax)S.pv=S.pvMax;S.esqBonus=b.esqFlat;}
function accrocherTrophy(id){if(!S.worn)S.worn=[];if(S.worn.length>=BELT_MAX||S.worn.includes(id))return;S.worn.push(id);recalcTrophyStats();sfx('coin');rSac();}
function decrocherTrophy(id){if(!S.worn)return;S.worn=S.worn.filter(x=>x!==id);recalcTrophyStats();sfx('clic');rSac();}
function newRun(cls){const c=CLASSES[cls];S={cls,pv:c.pv,pvMax:c.pv,or:60,tox:0,sac:{...c.depart},rep:0,primes:[],primesRemplies:0,kills:0,etape:0,beni:false,flags:{},trophies:[],worn:[],esqBonus:0,map:genMap(),cur:0,visited:[0],eventsVus:[]};C=null;ND=null;recalcTrophyStats();}
function sauvegarder(){try{localStorage.setItem(KEY,JSON.stringify(S));}catch(e){}}
function charger(){try{return JSON.parse(localStorage.getItem(KEY));}catch(e){return null;}}
function typeCol(c,i){if(c===0||c===5||c===10)return 'village';if(c===11)return 'nid';if((c===3||c===7)&&i===0)return 'taniere';const r=Math.random();return r<.48?'combat':r<.8?'event':'tresor';}
function genMap(){const N=12,nodes=[],edges=[];let id=0;const noms={0:{fr:"Bois-aux-Pendus",en:"Hanged Man’s Wood"},5:{fr:"Mare-aux-Corbeaux",en:"Crow’s Mire"},10:{fr:"Claywich",en:"Claywich"}};const isVillage=c=>c===0||c===5||c===10;const isNid=c=>c===11;for(let c=0;c<N;c++){const single=isVillage(c)||isNid(c);const k=single?1:2;nodes[c]=[];for(let i=0;i<k;i++){const type=typeCol(c,i);const nd={id:id++,c,i,type,m:null,enc:null};if(type==='combat'||type==='taniere'){nd.enc=pickRencontre(c,type);nd.m=nd.enc.lead;}if(type==='nid'){nd.enc={foes:['griffon'],lead:'griffon'};nd.m='griffon';}if(type==='village')nd.nom=noms[c];if(type!=='village'&&type!=='nid')nd.poi=pick(POI);nodes[c].push(nd);}}for(let c=0;c<N-1;c++){const A=nodes[c],B=nodes[c+1];if(isVillage(c+1)||isNid(c+1)){A.forEach(a=>edges.push([a.id,B[0].id]));}else if(isVillage(c)){B.forEach(b=>edges.push([A[0].id,b.id]));}else{B.forEach(b=>edges.push([pick(A).id,b.id]));A.forEach(a=>{if(!edges.some(e=>e[0]===a.id))edges.push([a.id,pick(B).id]);});}}return{nodes,edges};}
function trouveNoeud(id){for(const col of S.map.nodes)for(const nd of col)if(nd.id===id)return nd;return null;}
function nextIds(){return S.map.edges.filter(e=>e[0]===S.cur).map(e=>e[1]);}
const INNER_H=1180;
function coord(nd){const y=7+(nd.c/11)*85;const single=(S.map.nodes[nd.c].length===1);let x;if(single){x=50;}else{const bx=50+24*Math.sin(nd.c*0.85+0.5);x=bx+(nd.i===0?-15:15);}x=Math.max(14,Math.min(86,x));return{x,y};}
function ndLabel(nd){if(nd.type==='village')return t(nd.nom);if(nd.type==='nid')return t('ui.boss.nest');return t(nd.poi);}
function ciblePrimeDepuis(id){const file=[id],vu=new Set([id]);while(file.length){const cur=file.shift();for(const [a,b] of S.map.edges){if(a===cur&&!vu.has(b)){const nd=trouveNoeud(b);if((nd.type==='combat'||nd.type==='taniere')&&!S.visited.includes(b))return nd;vu.add(b);file.push(b);}}}return null;}
function hasItem(id){return (S.sac[id]||0)>0;}
function ouvrirSacHud(){if(C&&!C.fini)return;sfx('clic');_FROM_SAC=SCENE?SCENE.build:null;rSac();}
function fermerSac(){sauvegarder();const fn=_FROM_SAC;_FROM_SAC=null;if(fn){fn();}else{retourCarte();}}
function ouvrirBestiaire(){if(C&&!C.fini)return;sfx('clic');_FROM_BEST=SCENE?SCENE.build:null;rBestiaire();}
function fermerBestiaire(){const fn=_FROM_BEST;_FROM_BEST=null;if(fn){fn();}else{retourCarte();}}
function rHud(){const hud=$('hud');if(!S||!S.cls){hud.hidden=true;return;}hud.hidden=false;let row2='';if(C&&!C.fini){const ch=CHIPS;const h2=[];if(C.hst.bouclier>0)h2.push(t(ch.shield)+C.hst.bouclier);if(C.hst.parade)h2.push(t(ch.parry));if(C.hst.foudre>0)h2.push(t(ch.focus[S.cls])+' '+C.hst.foudre);if(C.hst.chat>0)h2.push(t(ch.evade[S.cls])+' '+C.hst.chat);if(C.hst.cri>0)h2.push(t(ch.cri)+C.hst.cri);if(C.hst.riposte)h2.push(t(ch.riposte));if(C.rune)h2.push(t(ch.runeF));if(C.hst.vampire>0)h2.push(t(ch.runeB)+C.hst.vampire);if(C.hst.poisonHN>0)h2.push(t(ch.empo));if(C.hst.peur>0)h2.push(t(ch.fear));if(C.hst.sonne>0)h2.push(t(ch.sonne));if(C.huile)h2.push('🧴 '+t(HUILES[C.huile]));if(C.signeCd>0)h2.push(t({fr:'✦ repos '+C.signeCd,en:'✦ rest '+C.signeCd}));if(C.hst.deséquilibre>0)h2.push(t({fr:'🌀 déséquilibré',en:'🌀 off-balance'}));row2=`<div class="hud2"><span class="pa-mini" title="${t('ui.combat.pa')}${C.tour}">${'◆'.repeat(C.pa)}${'◇'.repeat(Math.max(0,2-C.pa))}</span>${h2.map(c=>`<span class="chip mini-chip">${c}</span>`).join('')}</div>`;}hud.innerHTML=`${portrait('hero',S.cls,ico('wolf'),'med'+(C&&!C.fini?' pouls':''))}<div class="pv"><div class="jauge"><i style="width:${Math.max(0,S.pv/S.pvMax*100)}%"></i><span>${ico('heart')} ${S.pv}/${S.pvMax}</span></div></div><span class="stat">${ico('coin')} ${S.or}</span><button onclick="ouvrirSacHud()" style="background:none;border:none;padding:0;cursor:pointer" title="${t('ui.sac.title')}"><img src="assets/v_bag.png" alt="" style="width:30px;height:30px;object-fit:cover;border-radius:7px;border:1px solid #6b5a34;filter:drop-shadow(0 0 6px rgba(201,162,75,.5))"></button><button onclick="ouvrirBestiaire()" style="background:none;border:none;padding:0;cursor:pointer;font-size:1.5rem;line-height:1;filter:drop-shadow(0 0 6px rgba(195,155,216,.55))" title="${t('ui.best.title')}">📖</button>${(S.worn&&S.worn.length)?`<span class="stat">🏆${S.worn.length}/${BELT_MAX}</span>`:''}${S.rep?`<span class="stat">★${S.rep}</span>`:''}${row2}`;}
function rTitre(){document.body.classList.remove('in-game');S=null;C=null;scene(rTitre);rHud();const sv=charger();ec.innerHTML=cineBg('cin_title')+`<div class="ecran titre-pro"><div class="tp-seal">${ico('wolf')}</div><div class="tp-sur">${t('ui.title.surtitle')}</div><h1 class="tp-h1">LA GRANDE<span>ROUTE</span></h1><div class="tp-act"><span></span>${t('ui.title.act')}<span></span></div><div class="tp-lore">${t('ui.title.lore')}</div><div class="tp-road">${t('ui.title.signL')}&nbsp;·&nbsp;${t('ui.title.signR')}</div><div class="tp-btns">${sv?`<button class="btn btn-or gros" onclick="reprendre()">${t('ui.title.resume')}${sv.etape}/11</button><button class="btn" onclick="document.body.classList.add('in-game');rClasses()">${t('ui.title.begin')}</button><button class="btn btn-sang tp-brule" onclick="localStorage.removeItem('${KEY}');rTitre()">${t('ui.title.burn')}</button>`:`<button class="btn btn-or gros" onclick="document.body.classList.add('in-game');rClasses()">${t('ui.title.begin')}</button>`}</div><div class="tp-foot">${t('ui.title.version')}</div></div>`;}
function reprendre(){const sv=charger();if(!sv)return rTitre();document.body.classList.add('in-game');S=sv;S.trophies=S.trophies||[];S.worn=S.worn||[];S.flags=S.flags||{};S.sac=S.sac||{};S.eventsVus=S.eventsVus||[];C=null;recalcTrophyStats();rCarte();}
function rClasses(){sfx('clic');scene(rClasses);let html=`<div class="ecran"><h2>${t('ui.class.head')}</h2><p class="f-fell" style="color:#9a8a64;margin:.3rem 0 .6rem">${t('ui.class.sub')}</p>`;for(const id in CLASSES){const c=CLASSES[id];html+=`<div class="classe" style="--c:${c.coul}"><div onclick="zoomHero('${id}')" style="cursor:pointer" title="🔍 ${t(c.n)}">${portrait('hero',id,c.em,'cl-em')}</div><div><h3>${t(c.n)}</h3><div class="cl-ec">${t(c.ec)}</div><p>${t(c.desc)}</p><div class="cl-stats">${Object.entries(c.st).map(([k,v])=>`<div><label>${t(STLAB[k])}<span>${v}</span></label><div class="mini"><i style="width:${v/15*100}%"></i></div></div>`).join('')}</div><div class="cl-spec">✦ ${t(c.spec)}</div><div class="cl-cri f-fell">${t(c.cri)}</div></div><div class="cl-choix"><button class="btn btn-or" onclick="choisir('${id}')">${t('ui.class.choose')}</button></div></div>`;}ec.innerHTML=html+'</div>';}
function choisir(id){sfx('cor');document.body.classList.add('in-game');CURCLS=id;newRun(id);viewPrologue();}
function viewPrologue(){scene(viewPrologue);ec.innerHTML=`<div class="ecran"><div class="parch" style="max-width:420px;margin:2.5rem auto"><div style="display:flex;gap:.8rem;align-items:center;margin-bottom:.6rem">${portrait('npc','ealdorman','🧓','npc')}<h3>${t('ui.prologue.head')}</h3></div><div class="orn">✦ ✦ ✦</div><p class="q">${t(PROLOGUES[CURCLS])}</p><div style="margin-top:1rem"><button class="btn btn-or" onclick="rVillage(trouveNoeud(0))">${t('ui.prologue.enter')}</button></div></div></div>`;}
const ICONES={combat:'⚔️',event:'❓',tresor:'💰',village:'🏘️',taniere:'🐾',nid:'👑'};
function rCarte(){C=null;scene(rCarte);rHud();const dispo=nextIds();let html=`<div class="ecran"><div class="carte-tete f-cin"><b>${t('ui.map.title')}</b><span>${t('ui.map.step')}${S.etape}/11</span></div><div class="carte" id="carte"><div class="carte-inner" id="carteInner" style="height:${INNER_H}px"><svg id="lignes"></svg>`;S.map.nodes.forEach(col=>{col.forEach(nd=>{const p=coord(nd);const estDispo=dispo.includes(nd.id),courant=nd.id===S.cur,fait=S.visited.includes(nd.id)&&!courant;const prime=S.primes.some(pp=>pp.nodeId===nd.id&&!pp.done);html+=`<button id="n${nd.id}" class="noeud ${nd.type==='nid'?'boss':''} ${fait?'fait':''} ${courant?'courant':''} ${estDispo?'dispo':''}" style="left:${p.x}%;top:${p.y}%" ${estDispo?`onclick="aller(${nd.id})"`:'disabled'} title="${nd.type}"><span class="nd-ico">${ICONES[nd.type]}</span>${prime?'<i class="pin">📜</i>':''}<span class="nd-label">${ndLabel(nd)}</span></button>`;});});html+=`</div></div><div class="legende">${t('ui.map.legend').map(l=>`<span>${l}</span>`).join('')}</div></div>`;ec.innerHTML=html;requestAnimationFrame(()=>{dessinerLignes();frameCurrent();});}
function dessinerLignes(){const inner=$('carteInner'),svg=$('lignes');if(!inner||!svg)return;const rc=inner.getBoundingClientRect();svg.setAttribute('width',inner.offsetWidth);svg.setAttribute('height',inner.offsetHeight);let h='';S.map.edges.forEach(([a,b])=>{const na=$('n'+a),nb=$('n'+b);if(!na||!nb)return;const ra=na.getBoundingClientRect(),rb=nb.getBoundingClientRect();const x1=ra.left+ra.width/2-rc.left,y1=ra.top+ra.height/2-rc.top;const x2=rb.left+rb.width/2-rc.left,y2=rb.top+rb.height/2-rc.top;const cls=S.visited.includes(a)&&S.visited.includes(b)?'l-fait':(a===S.cur?'l-dispo':'l-futur');h+=`<path d="M${x1},${y1} C${x1},${(y1+y2)/2} ${x2},${(y1+y2)/2} ${x2},${y2}" class="${cls}"/>`;});svg.innerHTML=h;}
function frameCurrent(){const carte=$('carte'),inner=$('carteInner'),cur=$('n'+S.cur);if(!carte||!inner||!cur)return;const ri=inner.getBoundingClientRect(),rc=cur.getBoundingClientRect();const yInInner=rc.top-ri.top+rc.height/2;carte.scrollTop=Math.max(0,yInInner-carte.clientHeight/2);}
function aller(id){if(!S.map.edges.some(e=>e[0]===S.cur&&e[1]===id))return;S.cur=id;S.visited.push(id);S.etape=trouveNoeud(id).c;sfx('clic');resoudre(trouveNoeud(id));}
function resoudre(nd){switch(nd.type){case 'village':rVillage(nd);break;case 'combat':startCombat(nd.enc,{prime:trouverPrime(nd.id)});break;case 'taniere':startCombat(nd.enc,{elite:true,prime:trouverPrime(nd.id)});break;case 'event':{if(S.flags.pendu_maudit&&!S.flags.pendu_fait&&nd.c>=2){S.flags.pendu_fait=true;scene(viewPenduSpectre);viewPenduSpectre();break;}const dispo=EVENTS.filter(ev=>!S.eventsVus.includes(ev.titre));if(dispo.length){const ev=pick(dispo);S.eventsVus.push(ev.titre);rEvent(ev);}else{rTresor();}}break;case 'tresor':rTresor();break;case 'nid':rIntroBoss();break;}}
function viewPenduSpectre(){rHud();ec.innerHTML=`<div class="ecran"><div class="parch" style="max-width:440px;margin:1.5rem auto"><h3>👻 ${t('w.quest.pendu.spectreTitle')}</h3><div class="orn">✦ ✦ ✦</div><p class="q">${t('w.quest.pendu.intro')}</p><div style="margin-top:1rem"><button class="btn btn-sang gros" onclick="startCombat({foes:['spectre'],lead:'spectre'},{});if(C)C.log.push(t('w.quest.pendu.spectre'))">${t('ui.face')}</button></div></div></div>`;}
function trouverPrime(nodeId){return S.primes.find(p=>p.nodeId===nodeId&&!p.done)||null;}
function retourCarte(){sauvegarder();if(ND&&ND.c===0&&!S.flags.pendu_resolu){S.flags.pendu_resolu=true;if(!S.flags.pendu_decroche)S.flags.pendu_maudit=true;}rCarte();}
function genPrimes(nd){if(nd.primesGen)return;nd.primesGen=true;nd.primesV=[];const cible=ciblePrimeDepuis(nd.id);if(cible){const elite=cible.type==='taniere';nd.primesV.push({nodeId:cible.id,m:cible.m,rec:(elite?120:70)+S.etape*5,elite,pris:false});}}
function villageNpcBlock(nd){if(nd.c!==5)return '';const done=!!S.flags.woodcutter_done;const has=hasItem('bague');let body;if(done){body=`<p class="q">${t('w.npc.woodcutter.done')}</p>`;}else{body=`<p class="q">${t('w.npc.woodcutter.txt')}</p>`+(has?`<div style="margin-top:.7rem"><button class="btn btn-or" onclick="rendreBague()">${t('w.npc.woodcutter.give')}</button></div>`:`<p class="f-fell" style="color:#7a5210;margin-top:.5rem">${t('w.npc.woodcutter.noring')}</p>`);}return `<div class="panneau section"><div style="display:flex;gap:.8rem;align-items:center;margin-bottom:.5rem">${portrait('npc','woodcutter','🪓','npc')}<h3 style="margin:0">🪓 ${t('w.npc.woodcutter.name')}</h3></div>${body}</div>`;}
function villageQuestBlock(nd){if(nd.c===0){const done=!!S.flags.pendu_decroche;let body;if(done){body=`<p class="q">${t('w.quest.pendu.done')}</p>`;}else{body=`<p class="q">${t('w.quest.pendu.txt')}</p><div style="margin-top:.7rem"><button class="btn btn-or" onclick="decrocherPendu()">${t('w.quest.pendu.btn')}</button></div>`;}return `<div class="panneau section"><h3>🪢 ${t('w.quest.pendu.name')}</h3>${body}</div>`;}if(nd.c===10){return `<div class="panneau section"><h3>📯 ${t('w.quest.baron.name')}</h3><p class="q">${t('w.quest.baron.txt')}</p></div>`;}return '';}
function decrocherPendu(){if(S.flags.pendu_decroche)return;S.flags.pendu_decroche=true;S.rep--;sfx('clic');rVillage(ND);}
function rendreBague(){if(!hasItem('bague')||S.flags.woodcutter_done)return;S.sac.bague=Math.max(0,(S.sac.bague||0)-1);if(S.sac.bague<=0)delete S.sac.bague;S.flags.woodcutter_done=true;S.rep++;S.or+=60;sfx('coin');rVillage(ND);}
function rVillage(nd){ND=nd;genPrimes(nd);scene(()=>rVillage(ND));rHud();let decime=false;if(S.flags.fleau_epargne){decime=true;S.flags.village_decime_nom=t(nd.nom);delete S.flags.fleau_epargne;S.rep=Math.max(0,S.rep-1);}ND._fullRest=!!S.flags.puits_net;if(ND._fullRest)delete S.flags.puits_net;const rabais=Math.max(0,Math.min(.3,S.rep*.1));const surcout=Math.max(0,Math.min(.3,-S.rep*.1));const prix=it=>Math.round(it.p*(1-rabais+surcout));let stock=Object.entries(OBJETS).filter(([id,it])=>it.cls===S.cls);if(S.rep<0)stock=stock.slice(0,Math.max(1,stock.length-Math.min(2,-S.rep)));let html=bg('place','village')+`<div class="ecran"><div class="ville-tete">${portrait('place','village','🏘️','ville-em')}<h2>${t(nd.nom)}</h2><p class="f-fell" style="color:#9a8a64">${nd.c===10?t('ui.village.last'):t('ui.village.isle')}</p>${decime?`<div class="parch" style="margin:.7rem 0"><p class="q">🔥 ${t('ui.village.ruinedBanner')}</p></div>`:''}</div><div class="panneau section"><h3>${t('ui.village.inn')}</h3><button class="btn" onclick="repos()" ${nd.repos||decime?'disabled':''}>${decime?t('ui.village.innClosed'):(nd.repos?t('ui.village.rested'):(ND._fullRest?t('ui.village.restDeep'):t('ui.village.rest')))}</button></div>${villageNpcBlock(nd)}${villageQuestBlock(nd)}<div class="panneau section"><h3>${t('ui.village.counter')} ${rabais?`<span style="color:#8fae52">${t('ui.village.fameDisc')}${Math.round(rabais*100)}%)</span>`:''}${surcout?`<span style="color:#c85040">${t('ui.village.fameMalus')}${Math.round(surcout*100)}%)</span>`:''}</h3>${S.rep<0?`<p class="f-fell" style="color:#7a5210">${t('ui.village.stockLow')}</p>`:''}${decime?`<p class="f-fell" style="color:#7a5210">${t('ui.village.counterGone')}</p>`:stock.map(([id,it])=>`<div class="ligne-boutique">${vignette(it.img,it.em,'vig-shop')}<div class="inf"><b>${t(it.n)}</b><span>${t(it.d)}</span></div><button class="btn btn-or" onclick="acheter('${id}',${prix(it)})" ${S.or<prix(it)?'disabled':''}>${prix(it)}🪙</button></div>`).join('')}</div><div class="section"><h3>${t('ui.village.bounties')}</h3>${nd.primesV.length?nd.primesV.map((p,i)=>`<div class="parch prime"><span class="pin">📌</span><h3>PRIME : ${t(MONSTRES[p.m].n)} ${p.elite?'(tanière)':''}</h3><p class="q">${t('ui.village.bountyDesc')}</p><p class="rec">${t('ui.village.reward')}${p.rec}${t('ui.village.crowFame')}</p><div style="margin-top:.6rem"><button class="btn btn-or" onclick="accepterPrime(${i})" ${p.pris?'disabled':''}>${p.pris?t('ui.village.accepted'):t('ui.village.accept')}</button></div></div>`).join(''):`<p class="f-fell" style="color:#8d7d58">${t('ui.village.bountyEmpty')}</p>`}</div><button class="btn btn-or gros" onclick="retourCarte()">${t('ui.leave')}</button></div>`;ec.innerHTML=html;}
function repos(){if(ND.repos)return;ND.repos=true;S.pv=ND._fullRest?S.pvMax:Math.min(S.pvMax,S.pv+Math.round(S.pvMax*.5));sfx('soin');rVillage(ND);}
function acheter(id,prix){if(S.or<prix)return;sfx('coin');S.or-=prix;S.sac[id]=(S.sac[id]||0)+1;rVillage(ND);}
function accepterPrime(i){const p=ND.primesV[i];if(!p||p.pris)return;p.pris=true;S.primes.push({nodeId:p.nodeId,m:p.m,rec:p.rec,done:false});sfx('coin');rVillage(ND);}
function rEvent(ev){EV=ev;scene(()=>rEvent(EV));rHud();ec.innerHTML=`<div class="ecran"><div class="parch" style="max-width:440px;margin:1.5rem auto">${ev.npc?`<div style="text-align:center;margin-bottom:.4rem">${portrait('npc',ev.npc,'🧙','npc')}</div>`:''}<h3>${ev.em} ${t(ev.titre)}</h3><div class="orn">✦ ✦ ✦</div><p class="q">${t(ev.txt)}</p><div class="choix">${ev.choix.map((c,i)=>({c,i})).filter(({c})=>!c.req||hasItem(c.req)).map(({c,i})=>`<button class="btn choixbtn" onclick="choixEvent(${i})"><b>${t(c.t)}</b>${t(c.d)}</button>`).join('')}</div></div></div>`;}
function choixEvent(i){sfx('clic');const __ch=EV.choix[i];const res=__ch.eff();if(__ch&&__ch.consume){S.sac[__ch.consume]=Math.max(0,(S.sac[__ch.consume]||0)-1);if(S.sac[__ch.consume]<=0)delete S.sac[__ch.consume];}if(res.heroique)S.rep++;if(res.or)S.or+=res.or;EV._res=res;EV._enc=res.combat?rencontreEvent(res.combat):null;scene(viewEventResult);viewEventResult();rHud();}
function viewEventResult(){const res=EV._res;ec.innerHTML=`<div class="ecran"><div class="parch resultat" style="max-width:440px;margin:1.5rem auto">${EV.npc?`<div style="text-align:center;margin-bottom:.4rem">${portrait('npc',EV.npc,'🧙','npc')}</div>`:''}<h3>${EV.em} ${t(EV.titre)}</h3><div class="orn">✦ ✦ </div><p class="q">${t(res.txt)}</p><div style="margin-top:1rem">${res.combat?`<button class="btn btn-sang gros" onclick="startCombat(EV._enc,{})">${t('ui.face')}</button>`:`<button class="btn btn-or" onclick="retourCarte()">${t('ui.cont')}</button>`}</div></div></div>`;}
function rTresor(){scene(rTresor);rHud();ec.innerHTML=`<div class="ecran"><div class="parch" style="max-width:440px;margin:1.5rem auto"><h3>${t('ui.treasure.title')}</h3><div class="orn">✦ ✦ ✦</div><p class="q">${t('ui.treasure.txt')}</p><div class="choix"><button class="btn choixbtn" onclick="ouvrirTresor(false)"><b>${t('ui.treasure.force')}</b>${t('ui.treasure.forceD')}</button><button class="btn choixbtn" onclick="ouvrirTresor(true)"><b>${t('ui.treasure.pick')}${S.cls==='eclaireuse'?t('ui.treasure.pickElf'):''}</b>${t('ui.treasure.pickD')}</button><button class="btn choixbtn" onclick="retourCarte()"><b>${t('ui.treasure.leave')}</b></button></div></div></div>`;}
function rollLootStr(){const r=Math.random();if(r<.5){const o=R(45,95);S.or+=o;return t('w.treasure.lootGold',{or:o});}else if(r<.8){const id=soinClasse();S.sac[id]=(S.sac[id]||0)+1;return t('w.treasure.lootVial',{name:t(OBJETS[id].n)});}else{const a=itemClasse(),b=itemClasse();S.sac[a]=(S.sac[a]||0)+1;S.sac[b]=(S.sac[b]||0)+1;return t('w.treasure.lootGear',{a:t(OBJETS[a].n),b:t(OBJETS[b].n)});}}
function ouvrirTresor(crochet){sfx('clic');const ambushEnc=()=>{const m=pick(['noyeur','pourrissoir','goule']);return Math.random()<.4?{foes:[m,m],lead:m}:{foes:[m],lead:m};};let txt='',combat=null;if(crochet){const open=Math.random()<(S.cls==='eclaireuse'?.85:.6);const ambush=Math.random()<.45;txt=open?(t('w.treasure.pickOpen')+' '+rollLootStr()):t('w.treasure.pickFail');if(ambush){txt+=' '+t('w.treasure.ambush');combat=ambushEnc();}}else{const open=Math.random()<.5;if(open){txt=t('w.treasure.forceOpen')+' '+rollLootStr();}else{S.pv=Math.max(1,S.pv-8);txt=t('w.treasure.forceTrap');}}if(!combat&&!hasItem('bague')&&!S.flags.woodcutter_done&&S.etape<=5&&Math.random()<0.3){S.sac.bague=(S.sac.bague||0)+1;txt+=' '+t('w.treasure.foundRing');}S._tresor={txt,combat};scene(viewTresorResult);viewTresorResult();rHud();}
function viewTresorResult(){const r=S._tresor||{txt:''};ec.innerHTML=`<div class="ecran"><div class="parch resultat" style="max-width:440px;margin:1.5rem auto"><h3>${t('ui.treasure.resTitle')}</h3><div class="orn">✦ ✦ ✦</div><p class="q">${r.txt}</p><div style="margin-top:1rem">${r.combat?`<button class="btn btn-sang gros" onclick="startCombat(S._tresor.combat,{})">${t('ui.face')}</button>`:`<button class="btn btn-or" onclick="retourCarte()">${t('ui.cont')}</button>`}</div></div></div>`;}
function makeFoe(key,i,eliteIdx){const M=MONSTRES[key];const elite=(i===eliteIdx);let pv=R(M.pv[0],M.pv[1]),atq=M.atq.slice();if(elite){pv=Math.round(pv*1.15);atq=[atq[0]+2,atq[1]+2];}return{key,em:M.em,nom:M.n,pvE:pv,pvEMax:pv,atq,faib:M.faib,cat:M.cat,vole:!!M.vole,boss:!!M.boss,epic:!!M.epic,trophy:M.trophy||null,special:M.special||null,elite,chute:0,sol:0,st:{},intent:null,vivant:true};}
function startCombat(enc,opts={}){const eliteIdx=opts.elite?0:-1;const foes=enc.foes.map((k,i)=>makeFoe(k,i,eliteIdx));C={foes,cible:0,prime:opts.prime||null,isLair:!!opts.elite,hst:{bouclier:0,parade:false,foudre:0,chat:0,regen:0,regenN:0,poisonH:0,poisonHN:0,peur:0,sonne:0,cri:0,riposte:false,vampire:0,beni:false,deséquilibre:0},pa:2,tour:1,log:[],fini:false,objets:false,huile:null,rune:false,meteore:false,signeCd:0,enrage:false,grabUsed:false,hadBoss:foes.some(e=>e.boss),leadEm:MONSTRES[enc.lead].em};if(S.beni){C.hst.beni=true;S.beni=false;}const wb=aggregateWorn();if(wb.armorStart)C.hst.bouclier+=wb.armorStart;if(wb.paFirst)C.pa+=wb.paFirst;C.bonusDmgCat=wb.dmgCat;const noms=[...new Set(foes.map(e=>t(e.nom)))].join(' + ');C.log.push(ico('swords')+' '+noms+' — '+t(MONSTRES[enc.lead].txt));C.log.push(t(pick(TEXTES[S.cls].degaine)));if(C.prime)C.log.push(t('w.log.bountyActive',{n:t(MONSTRES[enc.lead].n),r:C.prime.rec}));pickIntents();rCombat();rHud();}
function aliveFoes(){return C.foes.filter(e=>e.vivant);}
function curFoe(){return (C&&C.foes)?C.foes[C.cible]:null;}
function recible(){if(!C)return;if(!C.foes[C.cible]||!C.foes[C.cible].vivant){const a=aliveFoes();if(a.length)C.cible=C.foes.indexOf(a[0]);}}
function setCible(i){if(!C||C.fini)return;if(C.foes[i]&&C.foes[i].vivant){C.cible=i;sfx('clic');rCombat();}}
const SPECIAL_FX={devour:1,rally:1,swoop:1,brume:1};
function specialReady(foe){if(!foe.special)return false;switch(foe.special.fx){case 'devour':return foe.pvE<foe.pvEMax&&C.foes.some(a=>a.vivant&&a!==foe&&a.cat==='necrophage');case 'rally':return !foe.rallyUsed&&C.foes.some(a=>a!==foe&&a.key==='nekkere'&&!a.vivant);case 'swoop':return !!foe.vole&&!C.grabUsed;}return false;}
function pickIntents(){C.foes.forEach(e=>{if(!e.vivant)return;const M=MONSTRES[e.key];if(e.special&&specialReady(e)&&Math.random()<0.7){e.intent={n:e.special.n,em:e.special.em,fx:e.special.fx,d:[0,0]};return;}const pool=e.boss?(e.vole?M.intsVol:M.intsSol):M.ints;e.intent=pick(pool);});}
function execSpecial(foe){const fx=foe.intent.fx;
if(fx==='brume'){foe.st.intangible=4;log(t('w.log.intangible',{n:t(foe.nom)}));pushFx('ennemi','🌫️','#9fc0dd');return;}
if(fx==='devour'){const prey=C.foes.filter(a=>a.vivant&&a!==foe&&a.cat==='necrophage').sort((a,b)=>a.pvE-b.pvE)[0];if(prey){prey.vivant=false;const h=Math.round(prey.pvE*0.5)+12;foe.pvE=Math.min(foe.pvEMax,foe.pvE+h);log(t('w.log.devour',{n:t(foe.nom),p:t(prey.nom),h}));pushFx('ennemi','+'+h,'#8fae52');sfx('mort');recible();return;}frappeEnnemi(foe);return;}
if(fx==='rally'){foe.rallyUsed=true;const n=R(1,3);for(let i=0;i<n;i++)C.foes.push(makeFoe('nekkere',C.foes.length,-1));log(t('w.log.rally',{n:t(foe.nom)}));pushFx('ennemi','📯','#c07a3c');sfx('cor');return;}
if(fx==='swoop'){C.grabUsed=true;let d=R(14,20);if(C.hst.bouclier>0){const abs=Math.min(C.hst.bouclier,d);C.hst.bouclier-=abs;d-=abs;log(t('w.log.shieldAbsorb',{a:abs,e:C.hst.bouclier<=0?t('w.log.shieldShatter'):t('w.log.shieldHolds')}));if(C.hst.bouclier<=0)pushFx('heros',t(FXW.quenb),'#9fc0dd');}if(d>0){S.pv=Math.max(0,S.pv-d);pushFx('heros','−'+d,'#e0604f');secouer('zone-heros');sfx('crit');log(t('w.log.swoop',{n:t(foe.nom),d}));}C.hst.sonne=2;foe.vole=false;foe.sol=2;log(t('w.log.swoopLand'));return;}
frappeEnnemi(foe);}
function pushFx(cible,txt,coul){FX.push({cible,txt,coul});}
function dechargeFx(){FX.forEach(fx=>{const z=$(fx.cible==='ennemi'?'zone-ennemi':'zone-heros');if(!z)return;const s=document.createElement('span');s.className='deg';s.textContent=fx.txt;s.style.color=fx.coul;s.style.left=(12+Math.random()*72)+'%';s.style.top=(14+Math.random()*34)+'%';z.appendChild(s);setTimeout(()=>s.remove(),1150);});FX=[];}
function secouer(id){const e=$(id);if(e){e.classList.remove('secousse');void e.offsetWidth;e.classList.add('secousse');}}
function faiblesseFlash(){const z=$('zone-ennemi');if(!z)return;const s=document.createElement('span');s.className='faiblesse';s.textContent=t(FXW.weak);z.appendChild(s);setTimeout(()=>s.remove(),1250);}
function desequilibreFlash(){const z=$('zone-heros');if(!z)return;const s=document.createElement('span');s.className='faiblesse';s.textContent=t({fr:'🌀 DÉSÉQUILIBRÉ',en:'🌀 OFF-BALANCE'});s.style.color='#c39bd8';s.style.textShadow='0 0 14px #7e4fa0,0 2px 4px #000';z.appendChild(s);setTimeout(()=>s.remove(),1250);}
function log(x){if(!C)return;C.log.push(typeof x==='string'?x:t(x));if(C.log.length>6)C.log.shift();}
function vampirisme(d){if(!C||!C.hst||C.hst.vampire<=0||d<=0)return;const s=Math.round(d*.5);S.pv=Math.min(S.pvMax,S.pv+s);C.hst.vampire--;log(t('w.log.bloodDrink',{s}));}
function tuerFoe(foe){foe.vivant=false;if(BEST.indexOf(foe.key)===-1){BEST.push(foe.key);sauverBest();}log(t('ui.end.foeDown',{n:t(foe.nom)}));if(aliveFoes().length===0)victoire();}
function calcDmg(base,elem,foe){let d=base,crit=false,faib=false;if(C.hst.cri>0)d*=1.5;if(C.hst.riposte){d*=1.6;C.hst.riposte=false;log(t('w.log.riposteSnap'));}if(C.hst.beni)d*=1.1;if(C.hst.peur>0)d*=.75;if(C.hst.sonne>0)d*=.6;if(C.hst.foudre>0)d*=1.5;if(C.rune)d*=1.5;if(C.huile&&C.huile===foe.cat)d*=1.5;if(C.bonusDmgCat&&C.bonusDmgCat[foe.cat])d*=C.bonusDmgCat[foe.cat];if(elem&&elem===foe.faib){d*=1.6;faib=true;}if(foe.sol>0&&!foe.vole)d*=1.3;if(Math.random()<.12){d*=1.5;crit=true;}return{d:Math.round(d),crit,faib};}
function blesserEnnemi(base,elem,label,aoe,melee,dispel){if(!C||C.fini)return 0;const targets=aoe?aliveFoes():(curFoe()?[curFoe()]:[]);let mainD=0;targets.forEach(foe=>{if(!C||C.fini)return;if(melee&&foe.vole){pushFx('ennemi',t(FXW.miss),'#93a0ab');log(t('w.log.fliesAway',{n:t(foe.nom)}));return;}const r=calcDmg(base,elem,foe);if(foe.st.intangible>0){if(dispel){foe.st.intangible=0;log(t('w.log.intangibleBreak',{n:t(foe.nom)}));pushFx('ennemi','✦','#9fc0dd');}else{r.d=Math.max(1,Math.round(r.d*.2));}}foe.pvE=Math.max(0,foe.pvE-r.d);pushFx('ennemi',(r.crit?t(FXW.crit):'−')+r.d,r.crit?'#ffd76a':'#e0604f');secouer('zone-ennemi');sfx(r.crit?'crit':'hit');if(r.faib)faiblesseFlash();log(t('w.log.hit',{l:label,n:t(foe.nom),d:r.d,crit:r.crit?t('w.log.critTag'):'',faib:r.faib?t('w.log.weakTag'):''}));if(!aoe)mainD=r.d;if(foe.pvE<=0)tuerFoe(foe);});return mainD;}
function frappeEnnemi(foe){const it=foe.intent;if(!it)return;const iname=t(it.n);let d=R(it.d[0],it.d[1]);if(foe.st.ralenti>0)d=Math.ceil(d*.5);if(foe.st.rallie>0)d=Math.ceil(d*1.3);if(C.enrage&&foe.boss)d=Math.ceil(d*1.25);const off=C.hst.deséquilibre>0;if(off)d=Math.ceil(d*1.3);if(d>0){let esq=CLASSES[S.cls].esq+(S.esqBonus||0)+(C.hst.chat>0?35:0);if(off)esq=Math.max(0,esq-30);if(Math.random()*100<esq){log(t('w.log.dodge',{n:iname}));pushFx('heros',t(FXW.dodge),'#d8e88a');return;}if(C.hst.parade){C.hst.parade=false;if(Math.random()<.65){log(t('w.log.parryOk',{n:iname}));pushFx('heros',t(FXW.parry),'#93a0ab');sfx('clic');if(S.cls==='loup'){C.hst.riposte=true;log(t('w.log.ripostePrimed'));}return;}log(t('w.log.parryMiss'));}if(C.hst.bouclier>0){const abs=Math.min(C.hst.bouclier,d);C.hst.bouclier-=abs;d-=abs;log(t('w.log.shieldAbsorb',{a:abs,e:C.hst.bouclier<=0?t('w.log.shieldShatter'):t('w.log.shieldHolds')}));if(C.hst.bouclier<=0)pushFx('heros',t(FXW.quenb),'#9fc0dd');}if(d>0){S.pv=Math.max(0,S.pv-d);pushFx('heros','−'+d,'#e0604f');secouer('zone-heros');sfx('hit');log(t('w.log.foeHits',{em:it.em,n:iname,d}));if(off&&!C._offLog){C._offLog=true;log(t('w.log.offBalance'));}}}if(it.fx==='poison'){C.hst.poisonH=5;C.hst.poisonHN=2;log(t('w.log.venomSpread'));}if(it.fx==='peur'){C.hst.peur=2;log(t('w.log.fearGrip'));}if(it.fx==='sonne'){C.hst.sonne=2;log(t('w.log.dazed'));}}
function utiliserObjet(id){const it=OBJETS[id];if(!it||it.cls!==S.cls||(S.sac[id]||0)<1||C.pa<1||C.fini)return;C.pa--;S.sac[id]--;if(it.tox)S.tox+=it.tox;const fx=it.effect&&EFFECTS[it.effect];if(fx)fx(it);else if(it.use)it.use();if(!C||C.fini)return;sfx(it.sfx||'soin');rCombat();rHud();}
function agir(a){if(!C||C.fini)return;recible();const cl=CLASSES[S.cls];if(a==='objets'){C.objets=!C.objets;rCombat();return;}if(C.pa<=0)return;const dist=cl.distance;switch(a){case 'frappe':{C.pa--;const e=curFoe();if(e&&e.vole&&!dist){log(t('w.log.swingAir'));pushFx('ennemi',t(FXW.miss),'#93a0ab');break;}const d=blesserEnnemi(R(cl.atq[0],cl.atq[1]),null,t(NOMS.frappe[S.cls]),false,!dist);vampirisme(d);if(dist&&e&&e.vivant&&Math.random()<.35){e.st.saignement={d:3,n:2};log(t('w.log.boltBleed'));}if(!C||C.fini)return;break;}case 'lourde':{if(C.pa<2)return;const aoeL=(S.cls==='loup'||S.cls==='mercenaire');const meleeL=!dist;if(!aoeL){const e=curFoe();if(e&&e.vole&&meleeL){log(t('w.log.heavyAir'));pushFx('ennemi',t(FXW.miss),'#93a0ab');C.pa-=2;break;}if(e&&Math.random()<.15&&!(e.st.etourdi>0)&&!(e.st.ralenti>0)){log(t('w.log.heavyDodge'));pushFx('ennemi',t(FXW.miss),'#93a0ab');C.pa-=2;break;}}C.pa-=2;const base=Math.round(R(cl.atq[0],cl.atq[1])*1.9);const d=blesserEnnemi(base,null,t(NOMS.lourde[S.cls]),aoeL,meleeL);vampirisme(d);if(aoeL&&C&&!C.fini){C.hst.deséquilibre=1;C._flashDes=true;C._offLog=false;log(t('w.log.whirlOpen'));}if(S.cls==='eclaireuse'){const e=curFoe();if(e&&e.vivant){e.st.saignement={d:3,n:2};log(t('w.log.preciseBleed'));}}if(!C||C.fini)return;break;}case 'parade':C.pa--;C.hst.parade=true;log(t('w.log.parryStance'));break;case 'cri':C.pa--;C.hst.cri=2;log(t('w.log.warCry'));sfx('cor');break;case 'rempart':C.pa--;C.hst.bouclier+=15;log(t('w.log.bulwark'));break;case 'meteore':if(C.meteore||C.pa<2)return;C.pa-=2;C.meteore=true;sfx('signe');blesserEnnemi(26,null,'☄️ '+t('w.log.meteorLabel'),true,false);S.pv=Math.max(1,S.pv-14);pushFx('heros','−14','#c39bd8');secouer('zone-heros');log(t('ui.combat.meteorCost'));if(S.pv<=0){defaite();return;}if(!C||C.fini)return;break;case 'aard':case 'igni':case 'quen':case 'yrden':case 'axii':if(!cl.signes.includes(a))return;if(C.signeCd>0)return;C.pa--;C.signeCd=(S.cls==='sorciere')?2:3;sfx('signe');lancerSigne(a);if(!C||C.fini)return;break;}rCombat();rHud();if(C&&C._flashDes){desequilibreFlash();C._flashDes=false;}}
function lancerSigne(s){const e=curFoe();const sw=S.cls==='sorciere';switch(s){case 'aard':{let d=sw?7:4;if(e&&e.vole&&e.vivant){e.chute++;d+=2;}blesserEnnemi(d,'aard','🌀 Aard',false,false);if(!C||C.fini)return;if(e&&e.vivant){if(e.vole){log(t('w.log.aardFly'));}else if(Math.random()<(sw?.75:.5)){e.st.etourdi=sw?2:1;log(t('w.log.aardStun'));}}break;}case 'igni':{let d=sw?10:6;if(CLASSES[S.cls].igniBoost)d+=3;blesserEnnemi(d,'igni','🔥 Igni',true,false);if(!C||C.fini)return;aliveFoes().forEach(fo=>{if(Math.random()<(sw?.7:.5)){fo.st.brulure={d:sw?4:3,n:sw?3:2};}});if(aliveFoes().length)log(t('w.log.igniBurn'));break;}case 'quen':C.hst.bouclier=sw?20:12;log(t('w.log.quenCast'));break;case 'yrden':{if(e&&e.vivant&&e.vole){e.chute++;log(t('w.log.yrdenFly'));blesserEnnemi(sw?5:3,'yrden','🪤 Yrden',false,false,true);}else{blesserEnnemi(sw?5:3,'yrden','🪤 Yrden',false,false,true);if(!C||C.fini)return;if(e&&e.vivant){e.st.ralenti=sw?3:2;if(sw&&Math.random()<.6){e.st.etourdi=1;log(t('w.log.aardStun'));}else log(t('w.log.yrdenSlow'));}}break;}case 'axii':{blesserEnnemi(sw?12:8,'axii','💫 Axii',false,false);if(!C||C.fini)return;if(e&&e.vivant){if(Math.random()<(sw?.7:.5)){e.st.confus=sw?2:1;log(t('w.log.axiiConf'));}else log(t('w.log.axiiResist'));}break;}}}
function finTour(){if(!C||C.fini)return;C.foes.forEach(foe=>{if(!foe.vivant)return;if(foe.st.brulure){foe.pvE=Math.max(0,foe.pvE-foe.st.brulure.d);log(t('w.log.burnTick',{n:t(foe.nom),d:foe.st.brulure.d}));pushFx('ennemi','−'+foe.st.brulure.d,'#e8934a');foe.st.brulure.n--;if(foe.st.brulure.n<=0)delete foe.st.brulure;if(foe.pvE<=0)tuerFoe(foe);}if(foe.vivant&&foe.st.poison){foe.pvE=Math.max(0,foe.pvE-foe.st.poison.d);log(t('w.log.poisonTick',{n:t(foe.nom),d:foe.st.poison.d}));pushFx('ennemi','−'+foe.st.poison.d,'#8fae52');foe.st.poison.n--;if(foe.st.poison.n<=0)delete foe.st.poison;if(foe.pvE<=0)tuerFoe(foe);}if(foe.vivant&&foe.st.saignement){foe.pvE=Math.max(0,foe.pvE-foe.st.saignement.d);log(t('w.log.bleedTick',{n:t(foe.nom),d:foe.st.saignement.d}));foe.st.saignement.n--;if(foe.st.saignement.n<=0)delete foe.st.saignement;if(foe.pvE<=0)tuerFoe(foe);}});if(!C||aliveFoes().length===0){return;}C.foes.forEach(foe=>{if(!foe.vivant||C.fini)return;if(foe.st.confus>0){foe.st.confus--;const d=R(6,10);foe.pvE=Math.max(0,foe.pvE-d);log(t('w.log.confTick',{n:t(foe.nom),d}));pushFx('ennemi','−'+d,'#c39bd8');if(foe.pvE<=0)tuerFoe(foe);}else if(foe.st.etourdi>0){foe.st.etourdi--;log(t('w.log.stunTick',{n:t(foe.nom)}));}else if(foe.intent&&foe.intent.fx&&SPECIAL_FX[foe.intent.fx])execSpecial(foe);else frappeEnnemi(foe);if(foe.st.rallie>0)foe.st.rallie--;if(foe.st.intangible>0)foe.st.intangible--;if(foe.st.ralenti>0)foe.st.ralenti--;});if(S.pv<=0){defaite();return;}if(!C||aliveFoes().length===0){return;}C.foes.forEach(foe=>{if(!foe.vivant||!foe.boss)return;if(foe.vole&&foe.chute>=2){foe.vole=false;foe.sol=3;foe.st.etourdi=Math.max(foe.st.etourdi||0,1);foe.chute=0;log(t('w.log.griffonCrash'));sfx('crit');}else if(!foe.vole){foe.sol--;if(foe.sol<=0&&!(foe.st.etourdi>0)){foe.vole=true;log(t('w.log.griffonRise'));}}if(!C.enrage&&foe.pvE<foe.pvEMax*.5){C.enrage=true;log(t('w.log.griffonRage'));sfx('cor');}});pickIntents();if(C.hst.regenN>0){S.pv=Math.min(S.pvMax,S.pv+C.hst.regen);C.hst.regenN--;log(t('w.log.regenTick',{d:C.hst.regen}));}if(C.hst.poisonHN>0){S.pv=Math.max(0,S.pv-C.hst.poisonH);C.hst.poisonHN--;log(t('w.log.venomBite',{d:C.hst.poisonH}));}const toxMax=S.cls==='mercenaire'?95:70;if(S.tox>=toxMax){S.pv=Math.max(0,S.pv-4);log(t('w.log.toxOverflow'));}S.tox=Math.max(0,S.tox-8);['foudre','chat','peur','sonne'].forEach(k=>{if(C.hst[k]>0)C.hst[k]--;});if(C.hst.cri>0)C.hst.cri--;if(C.signeCd>0)C.signeCd--;C.hst.deséquilibre=0;if(S.pv<=0){defaite();return;}C.tour++;C.pa=2;C.hst.parade=false;recible();rCombat();rHud();}
function foeChips(foe){const ch=CHIPS,out=[];if(foe.st.brulure)out.push(t(ch.burn));if(foe.st.poison)out.push(t(ch.poison));if(foe.st.saignement)out.push(t(ch.bleed));if(foe.st.etourdi)out.push(t(ch.stun));if(foe.st.ralenti)out.push(t(ch.slow));if(foe.st.conf)out.push(t(ch.conf));if(foe.st.rallie)out.push(t(ch.rally));if(foe.st.intangible)out.push(t(ch.intangible));return out;}
function rCombat(){if(!C)return;recible();scene(rCombat);const cl=CLASSES[S.cls];const moy=it=>it?Math.round((it.d[0]+it.d[1])/2):0;const dis=a=>C.fini||C.pa<a?'disabled':'';const disSigne=(C.fini||C.pa<1||C.signeCd>0)?'disabled':'';const itemsClasse=Object.entries(OBJETS).filter(([id,it])=>it.cls===S.cls);const aoeL=(S.cls==='loup'||S.cls==='mercenaire');let foesHtml=`<div class="foes">`;C.foes.forEach((foe,i)=>{const cible=(i===C.cible&&foe.vivant);const intentTxt=foe.vivant&&foe.intent?`${foe.intent.em} ${t(foe.intent.n)}${moy(foe.intent)>0?' ~'+moy(foe.intent):''}`:'—';foesHtml+=`<div class="foe ${foe.vivant?'':'mort'} ${cible?'cible':''}" ${foe.vivant?`onclick="setCible(${i})"`:''}>${foe.boss?`<span class="crown">${ico('crown')}</span>`:''}<div class="fintent">${intentTxt}</div>${portrait('monster',foe.key,foe.em,'bestiole '+(foe.vole?'vol':'sol'),foe.st.intangible>0?'opacity:.5;filter:blur(1px) saturate(.6)':'')}<div class="fnom">${t(foe.nom)}</div><div class="fbadges">${foe.elite?`<span class="badge sol">${t('ui.boss.elite')}</span>`:''}${foe.epic&&!foe.boss?`<span class="badge sol">${t('ui.boss.epic')}</span>`:''}${foe.boss?(foe.vole?`<span class="badge vol">${t('ui.boss.vol')}</span>`:`<span class="badge sol">${t('ui.boss.sol')}</span>`):''}${C.enrage&&foe.boss?`<span class="badge rage">${t('ui.boss.rage')}</span>`:''}</div><div class="jauge"><i style="width:${Math.max(0,foe.pvE/foe.pvEMax*100)}%"></i><span>${Math.max(0,foe.pvE)}/${foe.pvEMax}</span></div><div class="fchips">${foeChips(foe).map(c=>`<span class="chip">${c}</span>`).join('')}</div></div>`;});foesHtml+=`</div>`;let html=`<div class="ecran combat"><div class="zone-ennemi" id="zone-ennemi">${foesHtml}</div><div class="journal">${C.log.slice(-5).map(l=>`<div>› ${l}</div>`).join('')}</div><div class="zone-heros" id="zone-heros"><div class="actions"><button class="btn act" onclick="agir('frappe')" ${dis(1)}>${ico('swords')} ${t(NOMS.frappe[S.cls])}<small>1 PA${cl.distance?' · dist':''}</small></button><button class="btn act" onclick="agir('lourde')" ${dis(2)}>💥 ${t(NOMS.lourde[S.cls])}<small>2 PA${aoeL?(t({fr:' · ligne',en:' · line'})):' · ×1.9'}</small></button><button class="btn act" onclick="agir('parade')" ${dis(1)}>${t('ui.combat.parry')}<small>${t('ui.combat.parryD')}</small></button>${cl.criAction?`<button class="btn act" onclick="agir('cri')" ${dis(1)}>${t('ui.combat.cri')}<small>${t('ui.combat.criD')}</small></button><button class="btn act" onclick="agir('rempart')" ${dis(1)}>${t('ui.combat.rempart')}<small>${t('ui.combat.rempartD')}</small></button>`:''}${cl.meteore?`<button class="btn act" onclick="agir('meteore')" ${C.meteore||C.pa<2||C.fini?'disabled':''}>${t('ui.combat.meteor')}<small>${t('ui.combat.meteorD')}</small></button>`:''}<button class="btn act" onclick="agir('objets')">${C.objets?t('ui.combat.fold'):t('ui.combat.objects')}<small>0 PA</small></button><button class="btn act btn-sang" onclick="finTour()" ${C.fini?'disabled':''}>${t('ui.combat.endturn')}<small>${t('ui.combat.endturnD')}</small></button></div>${cl.signes.length?`<div class="signes">${cl.signes.map(s=>`<button class="signe s-${s}" title="${t(SIGNES[s].d)}${C.signeCd>0?(' — '+t({fr:'repos '+C.signeCd+' tour(s)',en:'resting '+C.signeCd+' turn(s)'})):''}" onclick="agir('${s}')" ${disSigne}><span>${SIGNES[s].em}</span><small>${t(SIGNES[s].n)}</small></button>`).join('')}</div>`:''}${C.objets?`<div class="objets">${itemsClasse.map(([id,it])=>`<button class="btn act" onclick="utiliserObjet('${id}')" ${(S.sac[id]||0)<1||C.pa<1||C.fini?'disabled':''}>${vignette(it.img,it.em,'vig-obj')}${t(it.n)} ×${S.sac[id]||0}<small>${t(it.d)}</small></button>`).join('')||`<span style="grid-column:1/-1;text-align:center;color:#8d7d58;font-size:.8rem">${t('ui.combat.empty')}</span>`}</div>`:''}</div></div>`;ec.innerHTML=html;dechargeFx();}
function victoire(){if(!C||C.fini)return;C.fini=true;sfx('mort');let or=0;C.foes.forEach(foe=>{or+=R(8,18)+(foe.elite?40:0)+(foe.boss?300:0);});S.or+=or;S.kills+=C.foes.length;S.pv=Math.min(S.pvMax,S.pv+6);const leadN=t(MONSTRES[C.foes[0].key].n);const vicLine=t(pick(TEXTES[S.cls].victoire));const lines={fr:[t('w.log.frayFalls',{name:leadN,line:vicLine}),t('w.log.loot',{or})],en:[t('w.log.frayFalls',{name:leadN,line:vicLine}),t('w.log.loot',{or})]};if(Math.random()<.3){const id=soinClasse();S.sac[id]=(S.sac[id]||0)+1;const dn=t(OBJETS[id].n);const drop=t('w.log.lootDrop',{em:OBJETS[id].em,name:dn});lines.fr.push(drop);lines.en.push(drop);}if(C.isLair&&!hasItem('bague')&&!S.flags.woodcutter_done&&S.etape<=5){S.sac.bague=(S.sac.bague||0)+1;const rl=t('w.treasure.foundRing');lines.fr.push(rl);lines.en.push(rl);}const dropped=[];C.foes.forEach(foe=>{if(foe.epic&&foe.trophy&&!(S.trophies||[]).includes(foe.trophy)){S.trophies=S.trophies||[];S.trophies.push(foe.trophy);dropped.push(foe.trophy);}});dropped.forEach(tid=>{const T=TROPHIES[tid];if(!T)return;const auto=(S.worn||[]).length<BELT_MAX;if(auto){S.worn=S.worn||[];S.worn.push(tid);}const nm=t(T.n),vo=t(T.voice);lines.fr.push(`🏆 <b>${nm}</b> — ${vo}${auto?'':(' '+t('ui.sac.beltFull'))}`);lines.en.push(`🏆 <b>${nm}</b> — ${vo}${auto?'':(' '+t('ui.sac.beltFull'))}`);});if(dropped.length)recalcTrophyStats();if(C.prime){C.prime.done=true;S.or+=C.prime.rec;S.rep++;S.primesRemplies++;const hon=t('w.log.bountyHonored',{rec:C.prime.rec});lines.fr.push(hon);lines.en.push(hon);}S._vlines=lines;if(C.hadBoss){rVictoireActe();return;}scene(viewVictory);viewVictory();rHud();}
function viewVictory(){ec.innerHTML=`<div class="ecran"><div class="panneau resultat" style="max-width:420px;margin:2rem auto;text-align:center"><div style="margin:0 auto">${portrait('monster',C.foes[0].key,C.leadEm,'bestiole','width:96px;height:122px')}</div><h2 style="color:#8fae52">${t('ui.end.victory')}</h2><div class="orn">✦ ✦ </div>${S._vlines[LANG].map(l=>`<p style="margin:.4rem 0">${l}</p>`).join('')}<div style="margin-top:1.1rem"><button class="btn btn-or gros" onclick="retourCarte()">${t('ui.leave')}</button></div></div></div>`;}
function defaite(){C=null;S.mort=true;localStorage.removeItem(KEY);scene(viewDefeat);viewDefeat();rHud();}
function viewDefeat(){ec.innerHTML=`<div class="ecran fin defaite"><span class="em">☠</span><h1>${t('ui.end.defeatTitle')}</h1><p class="f-fell" style="color:#a89772;max-width:340px;margin:1rem auto">${t('ui.end.defeatTxt')}</p>${statsFin()}<button class="btn btn-sang gros" style="max-width:320px;margin:0 auto" onclick="rTitre()">${t('ui.end.retry')}</button></div>`;}
function rVictoireActe(){C=null;S.fini=true;localStorage.removeItem(KEY);scene(viewActe);viewActe();rHud();}
function viewActe(){ec.innerHTML=cineBg('cin_title')+`<div class="ecran fin victoire"><span class="em">🪶</span><div class="surtitre">${t('ui.end.actDone')}</div><h1>${t('ui.end.actTitle')}</h1><p class="f-fell" style="color:#c9b995;max-width:360px;margin:1rem auto">${t('ui.end.actTxt')}</p>${statsFin()}<div class="parch" style="max-width:380px;margin:1rem auto"><p class="q">📯 ${t('w.act2.teaser')}</p></div><p class="f-cin" style="letter-spacing:.3em;color:#9a8348;font-size:.75rem;margin:1rem 0">${t('ui.end.actNext')}</p><button class="btn btn-or gros" style="max-width:320px;margin:0 auto" onclick="rTitre()">${t('ui.end.newroute')}</button></div>`;}
function statsFin(){return `<div class="stats-fin"><div><b>${S.kills}</b><span>${t('ui.end.sKills')}</span></div><div><b>${S.or}</b><span>${t('ui.end.sGold')}</span></div><div><b>${S.primesRemplies}</b><span>${t('ui.end.sBounties')}</span></div><div><b>${S.rep}</b><span>${t('ui.end.sFame')}</span></div></div>`;}
function rIntroBoss(){scene(rIntroBoss);rHud();const conseil={loup:S.sac.hdrag>0?'w.boss.counsel.loupOk':'w.boss.counsel.loupNo',sorciere:'w.boss.counsel.sorciere',mercenaire:'w.boss.counsel.mercenaire',eclaireuse:'w.boss.counsel.eclaireuse'}[S.cls];ec.innerHTML=cineBg('cin_nest')+`<div class="ecran"><div class="parch" style="max-width:440px;margin:2rem auto">${portrait('place','nid','👑','nid-banner')}<h3>${t('ui.boss.title')}</h3><div class="orn">✦ ✦ </div><p class="q">${t(TEXTES[S.cls].boss)}<br><br>${t('w.boss.desc')}</p><p style="margin:.8rem 0;font-size:.9rem">${t(conseil)}</p><p class="q" style="font-size:.85rem;color:#5a4420">${t('ui.boss.advice')}</p><div style="margin-top:1rem"><button class="btn btn-sang gros" onclick="startCombat({foes:['griffon'],lead:'griffon'},{})">${t('ui.boss.fight')}</button></div></div></div>`;}
function zoomImg(src,emoji,caption){const ov=document.createElement('div');ov.style.cssText='position:fixed;inset:0;z-index:130;background:rgba(5,4,2,0);display:flex;align-items:center;justify-content:center;cursor:pointer;padding:1rem;transition:background .28s ease';const box=document.createElement('div');box.style.cssText='transform:scale(.78);opacity:0;transition:transform .3s cubic-bezier(.2,.9,.3,1),opacity .3s ease;display:flex;flex-direction:column;align-items:center;gap:.8rem';if(src){const img=document.createElement('img');img.src=src;img.alt='';img.style.cssText='max-width:92vw;max-height:78vh;object-fit:contain;border-radius:14px;border:2px solid #6b5a34;box-shadow:0 20px 60px rgba(0,0,0,.85)';box.appendChild(img);}else{const s=document.createElement('div');s.textContent=emoji||'';s.style.cssText='font-size:9rem;line-height:1';box.appendChild(s);}if(caption){const cap=document.createElement('div');cap.textContent=caption;cap.style.cssText='font-family:Cinzel,serif;font-weight:700;color:#e8d5a0;letter-spacing:.12em;text-transform:uppercase;font-size:.8rem';box.appendChild(cap);}ov.appendChild(box);const close=()=>{box.style.transform='scale(.78)';box.style.opacity='0';ov.style.background='rgba(5,4,2,0)';setTimeout(()=>ov.remove(),280);};ov.onclick=close;document.body.appendChild(ov);requestAnimationFrame(()=>{ov.style.background='rgba(5,4,2,.9)';box.style.transform='scale(1)';box.style.opacity='1';});}
function zoomBestiole(id){const M=MONSTRES[id];if(!M)return;const src=IMG.monster&&IMG.monster[id];zoomImg(src,M.em,t(M.n));}
function zoomHero(id){const c=CLASSES[id];if(!c)return;const src=IMG.hero&&IMG.hero[id];zoomImg(src,c.em,t(c.n));}
function rBestiaire(){scene(rBestiaire);rHud();const ids=BEST.filter(id=>MONSTRES[id]);let html=`<div class="ecran"><div class="sac-head"><span style="font-size:1.5rem">📖</span><h2>${t('ui.best.title')}</h2><span class="sac-count">${ids.length}/${Object.keys(MONSTRES).length}</span></div>`;if(!ids.length){html+=`<div class="sac-empty">${t('ui.best.empty')}</div>`;}else{html+=ids.map(id=>{const M=MONSTRES[id];const weak=M.faib?(SIGNES[M.faib]?t(SIGNES[M.faib].n):M.faib):t('ui.best.none');return `<div class="panneau section"><div style="display:flex;gap:.7rem;align-items:center"><div onclick="zoomBestiole('${id}')" style="cursor:pointer" title="🔍 ${t(M.n)}">${portrait('monster',id,M.em,'bestiole','width:78px;height:100px;border-radius:12px')}</div><div><h3 style="margin:0">${t(M.n)}</h3><div class="cl-ec" style="color:#9a8348">${CATNOMS[M.cat]?t(CATNOMS[M.cat]):''}</div></div></div><p class="f-fell" style="color:#9a8a64;margin:.55rem 0">${t(M.txt)}</p><div class="chips" style="justify-content:flex-start"><span class="chip">✦ ${t('ui.best.weak')} ${weak}</span><span class="chip">${t('ui.st.pv')} ${M.pv[0]}–${M.pv[1]}</span><span class="chip">${t('ui.best.atk')} ${M.atq[0]}–${M.atq[1]}</span>${M.vole?`<span class="chip">${t('ui.best.fly')}</span>`:''}${M.epic?`<span class="chip">★ ${t('ui.boss.epic')}</span>`:''}${M.boss?`<span class="chip">👑</span>`:''}</div></div>`;}).join('');}html+=`<button class="btn btn-or gros" onclick="fermerBestiaire()">${t('ui.best.close')}</button></div>`;ec.innerHTML=html;}
function rSac(){scene(rSac);const ids=Object.keys(S.sac).filter(id=>(S.sac[id]||0)>0&&OBJETS[id]);const by=k=>ids.filter(id=>(OBJETS[id].kind||'combat')===k);const row=(id,k)=>`<div class="ligne-boutique ${k==='quest'?'quest':k==='trophy'?'trophy':''}">${vignette(OBJETS[id].img,OBJETS[id].em,'vig-shop')}<div class="inf"><b>${t(OBJETS[id].n)}</b><span>${t(OBJETS[id].d)}</span></div><span class="stat">×${S.sac[id]}</span></div>`;const block=(title,list,k)=>list.length?`<div class="panneau section"><h3>${title}</h3>${list.map(id=>row(id,k)).join('')}</div>`:'';const worn=(S.worn||[]).filter(id=>TROPHIES[id]);const reserve=(S.trophies||[]).filter(id=>TROPHIES[id]&&!worn.includes(id));const trophyRow=(id,isWorn)=>{const T=TROPHIES[id];const full=worn.length>=BELT_MAX;return `<div class="ligne-boutique trophy ${isWorn?'worn':''}">${vignette(T.img,'🏆','vig-shop')}<div class="inf"><b>${t(T.n)}</b><span>${t(T.desc)}</span></div>${isWorn?`<button class="btn-mini off" onclick="decrocherTrophy('${id}')">${t('ui.sac.unequip')}</button>`:`<button class="btn-mini" onclick="accrocherTrophy('${id}')" ${full?'disabled title="'+t('ui.sac.beltFull')+'"':''}>${t('ui.sac.equip')}</button>`}</div>`;};const belt=`<div class="panneau section"><h3>${t('ui.sac.trophy')} <span class="sac-beltcount">${worn.length}/${BELT_MAX}</span></h3>${worn.length?worn.map(id=>trophyRow(id,true)).join(''):`<p class="f-fell" style="color:#8d7d58">${t('ui.sac.noTrophy')}</p>`}</div>`+(reserve.length?`<div class="panneau section"><h3>${t('ui.sac.reserve')}</h3>${reserve.map(id=>trophyRow(id,false)).join('')}</div>`:'');const empty=!ids.length&&!worn.length&&!reserve.length?`<div class="sac-empty">${t('ui.sac.empty')}</div>`:'';ec.innerHTML=`<div class="ecran"><div class="sac-head">${ico('bag')}<h2>${t('ui.sac.title')}</h2><span class="sac-count">${ids.reduce((a,id)=>a+S.sac[id],0)}</span></div>${empty}${belt}${block(t('ui.sac.quest'),by('quest'),'quest')}${block(t('ui.sac.combat'),by('combat'),'combat')}<button class="btn btn-or gros" onclick="fermerSac()">${t('ui.sac.close')}</button></div>`;}
(function(){const L=$('lucioles');for(let i=0;i<11;i++){const s=document.createElement('span');s.className='luc';s.style.left=R(2,96)+'vw';s.style.top=R(15,90)+'vh';s.style.setProperty('--d',R(4,9)+'s');s.style.setProperty('--del',(Math.random()*6).toFixed(1)+'s');L.appendChild(s);}})();
window.addEventListener('resize',()=>{if($('carteInner'))dessinerLignes();});
(function boot(){const haveEmb=typeof LOCALES!=='undefined'&&LOCALES[LANG]&&Object.keys(LOCALES[LANG]).length>0;const ready=haveEmb?Promise.resolve():(typeof loadLang==='function'?loadLang(LANG):Promise.resolve());ready.then(()=>{if(typeof LOCALES!=='undefined')DICT=LOCALES[LANG]||{};updLang();if(!document.body.classList.contains('in-game'))rTitre();const sp=$('splash');if(sp){sp.classList.add('out');setTimeout(()=>{if(sp.parentNode)sp.parentNode.removeChild(sp);},820);}});})();